a = 5
b = 2.2
# complex type
c = 1j
d = "Hello"
e = True

# list
fruit_list = ["Banana", "Apple", "Cherry"]

# tuple
number_tuple = (1, 2, 3,4, 5, 6, 7)

# range
data = range(2)

# dictionary
dictionary = {
    "name": "John",
    "last_name": "Doe",
    "age": 16
}

# set
my_set = {
    "apple",
    "banana"
}

# frozen set
x = frozenset({
    "dog",
    "cat",
    "rabbit"
})

# bytes
byte = b"Hello"

# byte array
byte_array = bytearray(5)

# memory view
memory = memoryview(bytes(10))

# none
nothing = None

# setting specific types
name = str("John")
age = int(21)
payment = float(12.12)
grocery_list = list(("banana", "milk", "eggs"))
todo_list = tuple(("cook", "play the guitar", "code", "sleep", "wake up"))
salary_range = range(30)
dog_info = dict(
    name="Rowan",
    age=1
)

last_name = str()
last_name = 'Nada'